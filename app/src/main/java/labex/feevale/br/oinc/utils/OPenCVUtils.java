package labex.feevale.br.oinc.utils;

import android.content.Context;

/*import org.opencv.android.BaseLoaderCallback;
import org.opencv.android.LoaderCallbackInterface;
import org.opencv.android.OpenCVLoader;*/

/**
 * Created by 0118230 on 12/02/2015.
 */
public class OPenCVUtils {

    public static void startOpenCV(Context context){

        /*BaseLoaderCallback mOpenCVCallBack = new BaseLoaderCallback(context) {
            @Override
            public void onManagerConnected( int status ) {
                switch (status){
                    case LoaderCallbackInterface.SUCCESS:
                        Log.i("OPENCV", "SUCESS");
                        break;
                    default:
                        super.onManagerConnected(status);
                        break;
                }

            }
        };

        OpenCVLoader.initAsync(OpenCVLoader.OPENCV_VERSION_2_4_9, context, mOpenCVCallBack);*/
    }
}
